# Use shamefully-hoist for better compatibility with tools
shamefully-hoist=true

# Don't fail on peer dependency warnings
strict-peer-dependencies=false

# Auto-install peers
auto-install-peers=true
